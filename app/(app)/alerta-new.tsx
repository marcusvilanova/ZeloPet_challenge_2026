import { useRouter } from "expo-router";
import React, { useState } from "react";
import { Pressable, ScrollView, StyleSheet, Text, View } from "react-native";

import { Card, LoadingState, PrimaryButton, SectionTitle, TextField } from "@/components/zelo-ui";
import { ScreenContainer } from "@/components/screen-container";
import { useColors } from "@/hooks/use-colors";
import { useCreateAlerta, usePets } from "@/hooks/use-zelo";
import type { AlertType } from "@/types/api";

const types: { value: AlertType; label: string; emoji: string }[] = [{ value: "VACINA", label: "Vacina", emoji: "💉" }, { value: "CHECKUP", label: "Check-up", emoji: "🩺" }, { value: "RETENCAO", label: "Acompanhamento", emoji: "⏰" }, { value: "POS_CONSULTA", label: "Pós-consulta", emoji: "📋" }];
export default function NewAlertScreen() { const colors = useColors(); const router = useRouter(); const pets = usePets(); const create = useCreateAlerta(); const [petId, setPetId] = useState<number>(); const [tipo, setTipo] = useState<AlertType>("CHECKUP"); const [titulo, setTitulo] = useState(""); const [descricao, setDescricao] = useState(""); const [dataPrevista, setDataPrevista] = useState(""); const [error, setError] = useState(""); if (pets.isLoading) return <ScreenContainer className="p-5"><LoadingState label="Carregando seus pets..." /></ScreenContainer>; async function save() { setError(""); if (!petId) return setError("Selecione um pet."); if (!titulo.trim()) return setError("Informe o nome do cuidado."); if (!dataPrevista.trim()) return setError("Informe a data prevista."); try { await create.mutateAsync({ petId, tipo, titulo: titulo.trim(), descricao: descricao.trim() || undefined, dataPrevista: dataPrevista.trim() }); router.back(); } catch (err) { setError(err instanceof Error ? err.message : "Não foi possível criar o alerta."); } } return <ScreenContainer className="p-5"><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><Pressable onPress={() => router.back()}><Text style={[styles.back, { color: colors.primary }]}>‹ Voltar</Text></Pressable><SectionTitle title="Novo alerta" subtitle="Crie um lembrete de cuidado para seu pet" /><Card style={styles.card}><Text style={[styles.label, { color: colors.foreground }]}>Pet</Text><View style={styles.pets}>{(pets.data ?? []).map((pet) => <Pressable key={pet.id} onPress={() => setPetId(pet.id)} style={[styles.pet, { borderColor: petId === pet.id ? colors.primary : colors.border, backgroundColor: petId === pet.id ? `${colors.primary}12` : colors.background }]}><Text style={{ fontSize: 18 }}>{pet.especie === "GATO" ? "🐱" : pet.especie === "CAO" ? "🐶" : "🐾"}</Text><Text style={[styles.optionText, { color: colors.foreground }]}>{pet.nome}</Text></Pressable>)}</View><Text style={[styles.label, { color: colors.foreground }]}>Tipo de cuidado</Text><View style={styles.types}>{types.map((item) => <Pressable key={item.value} onPress={() => setTipo(item.value)} style={[styles.type, { borderColor: tipo === item.value ? colors.primary : colors.border, backgroundColor: tipo === item.value ? `${colors.primary}12` : colors.background }]}><Text style={{ fontSize: 21 }}>{item.emoji}</Text><Text style={[styles.optionText, { color: colors.foreground }]}>{item.label}</Text></Pressable>)}</View><TextField label="Título" value={titulo} onChangeText={setTitulo} placeholder="Ex.: Vacina antirrábica" /><TextField label="Descrição (opcional)" value={descricao} onChangeText={setDescricao} placeholder="Detalhes do cuidado" multiline numberOfLines={4} /><TextField label="Data prevista" value={dataPrevista} onChangeText={setDataPrevista} placeholder="AAAA-MM-DD" />{error ? <Text style={[styles.error, { color: colors.error }]}>{error}</Text> : null}<PrimaryButton label="Criar alerta" onPress={save} loading={create.isPending} /></Card></ScrollView></ScreenContainer>; }
const styles = StyleSheet.create({ content: { gap: 18, paddingBottom: 32 }, back: { fontWeight: "800", fontSize: 15 }, card: { gap: 15 }, label: { fontSize: 13, fontWeight: "700", marginBottom: -5 }, pets: { flexDirection: "row", flexWrap: "wrap", gap: 8 }, pet: { minWidth: "30%", minHeight: 55, borderRadius: 13, borderWidth: 1, alignItems: "center", justifyContent: "center", gap: 3, paddingHorizontal: 8 }, types: { flexDirection: "row", flexWrap: "wrap", gap: 8 }, type: { width: "48%", minHeight: 65, borderRadius: 13, borderWidth: 1, alignItems: "center", justifyContent: "center", gap: 3 }, optionText: { fontSize: 11, fontWeight: "700", textAlign: "center" }, error: { fontSize: 13, lineHeight: 18 }, });
